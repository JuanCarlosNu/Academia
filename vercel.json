{
  "projects": {
    "frontend": {
      "buildCommand": "npm run build",
      "outputDirectory": "build"
    }
  },
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/react"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
